{
  "uReact Signal": {
    "prefix": "ursignal",
    "body": [
      "const ${1:count} = signal<${2:number}>(${3:0});"
    ],
    "description": "Create a fine-grained uReact reactive scalar signal atom"
  },
  "uReact Computed Signal": {
    "prefix": "urcomputed",
    "body": [
      "const ${1:doubled} = computed(() => ${2:count.value * 2});"
    ],
    "description": "Create an auto-tracked, lazily evaluated computed signal"
  },
  "uReact useSignal Hook": {
    "prefix": "urusesignal",
    "body": [
      "const [${1:val}, set${1/(.*)/${1:/capitalize}/}] = useSignal(${2:count});"
    ],
    "description": "Subscribe component to a signal with zero parent re-renders"
  },
  "uReact Reactive Store": {
    "prefix": "urstore",
    "body": [
      "export const ${1:userStore} = createStore({",
      "  ${2:name}: '${3:Alex}',",
      "  ${4:age}: ${5:28},",
      "  ${6:isPro}: ${7:true},",
      "  ${8:increment}() {",
      "    this.${4:age}++;",
      "  }",
      "});"
    ],
    "description": "Create a deep proxy reactive store with direct mutations and $bind proxy"
  },
  "uReact useStore Hook": {
    "prefix": "urusestore",
    "body": [
      "const ${1:store} = useStore(${2:userStore});"
    ],
    "description": "Subscribe React component to a proxy store via useSyncExternalStore"
  },
  "uReact Form Store with Validation": {
    "prefix": "urform",
    "body": [
      "const ${1:form} = createFormStore({",
      "  initialValues: {",
      "    ${2:email}: '',",
      "    ${3:password}: ''",
      "  },",
      "  rules: {",
      "    ${2:email}: [rules.required('${4:Email is required}'), rules.email('${5:Invalid email}')],",
      "    ${3:password}: [rules.required('${6:Password is required}'), rules.minLength(8, '${7:Min 8 characters}')]",
      "  },",
      "  onSubmit: async (values) => {",
      "    ${8:// Submit payload}",
      "    await ${9:api.login}(values);",
      "  }",
      "});"
    ],
    "description": "Create a reactive form store with declarative validation rules and $bind proxy"
  },
  "uReact Scoped CSS Component": {
    "prefix": "urscoped",
    "body": [
      "<Scoped css={`",
      "  .${1:card} {",
      "    ${2:background: #1e293b;}",
      "    ${3:padding: 16px;}",
      "    ${4:border-radius: 8px;}",
      "  }",
      "  .${1:card}:hover {",
      "    ${5:transform: translateY(-2px);}",
      "  }",
      "`}>",
      "  <div className=\"${1:card}\">",
      "    $0",
      "  </div>",
      "</Scoped>"
    ],
    "description": "Render a component-scoped CSS block with automatic [data-scope] isolation"
  },
  "uReact useScopedCSS Hook": {
    "prefix": "urusescoped",
    "body": [
      "const { scopeProps, className } = useScopedCSS(`",
      "  .${1:wrapper} {",
      "    ${2:display: flex;}",
      "  }",
      "`);"
    ],
    "description": "Inject scoped CSS into <head> with automatic ref-counted cleanup on unmount"
  },
  "uReact State Watcher": {
    "prefix": "urwatch",
    "body": [
      "useWatchReactive(${1:source}, (${2:next}, ${3:prev}) => {",
      "  ${4:console.log(`Transition from ${prev} to ${next}`);}",
      "});"
    ],
    "description": "Watch signals, stores, or getter functions with (newValue, oldValue) without useEffect boilerplate"
  },
  "uReact React 19 useAction": {
    "prefix": "uraction",
    "body": [
      "const ${1:action} = useAction<${2:string}, ${3:any}>(",
      "  async (prev, ${4:input}) => {",
      "    return await ${5:api.submit}(${4:input});",
      "  },",
      "  ${6:null},",
      "  {",
      "    optimisticUpdate: (prev, ${4:input}) => ${7:input}",
      "  }",
      ");"
    ],
    "description": "React 19 unified useActionState + useOptimistic with typed payload and rollback"
  },
  "uReact Declarative <Show>": {
    "prefix": "urshow",
    "body": [
      "<Show>",
      "  <Show.When is={${1:condition}}>",
      "    ${2:<div>Admin Content</div>}",
      "  </Show.When>",
      "  <Show.Else>",
      "    ${3:<div>Standard Content</div>}",
      "  </Show.Else>",
      "</Show>"
    ],
    "description": "Clean declarative conditional control flow without nested ternaries"
  },
  "uReact Declarative <When>": {
    "prefix": "urwhen",
    "body": [
      "<When is={${1:condition}} fallback={${2:null}}>",
      "  $0",
      "</When>"
    ],
    "description": "Declarative single-condition wrapper with optional fallback slot"
  },
  "uReact Declarative <For>": {
    "prefix": "urfor",
    "body": [
      "<For each={${1:items}} fallback={${2:<p>No items found</p>}}>",
      "  {(${3:item}, ${4:index}) => (",
      "    <div key={${4:index}}>{${3:item}.${5:title}}</div>",
      "  )}",
      "</For>"
    ],
    "description": "Declarative list loop rendering without manual .map() and empty checks"
  },
  "uReact Declarative <Await>": {
    "prefix": "urawait",
    "body": [
      "<Await for={${1:promise}} fallback={${2:<div>Loading...</div>}}>",
      "  {(${3:data}) => (",
      "    <div>{${3:data}.${4:name}}</div>",
      "  )}",
      "</Await>"
    ],
    "description": "Direct async promise unwrapping in JSX without useEffect state ceremony"
  },
  "uReact SWR useQuery": {
    "prefix": "urquery",
    "body": [
      "const { data, loading, error, refetch } = useQuery(",
      "  '${1:cache-key}',",
      "  async () => {",
      "    const res = await fetch('${2:/api/endpoint}');",
      "    return res.json();",
      "  },",
      "  { staleTime: ${3:15000} }",
      ");"
    ],
    "description": "Global SWR request deduplication, memory caching, and focus revalidation in 1.8KB"
  },
  "uReact DevTools HUD Mount": {
    "prefix": "urdevtools",
    "body": [
      "<DevTools defaultOpen={false} position=\"${1|bottom-right,bottom-left,top-right|}\" />"
    ],
    "description": "Embed the Quantum DevTools HUD with 60 FPS meter, time-travel, and telemetry"
  },
  "uReact Register DevTools": {
    "prefix": "urregdevtools",
    "body": [
      "registerDevTools('${1:StoreName}', '${2|store,signal,form,computed|}', ${3:instance});"
    ],
    "description": "Register store, signal, or form for real-time inspection in Quantum DevTools HUD"
  }
}
